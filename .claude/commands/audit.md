Run a full dependency audit and fix vulnerabilities:

1. Run `npm audit` to list all vulnerabilities
2. Run `npm audit fix` to auto-fix safe updates
3. Run `npm run build` to verify nothing is broken after the fixes
4. Report a summary of what was fixed and what still needs manual attention
