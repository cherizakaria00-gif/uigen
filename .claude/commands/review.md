Review the following file or feature for quality issues: $ARGUMENTS

Check for:
1. **Security** — XSS, injection, exposed secrets, unsafe operations
2. **Performance** — unnecessary re-renders, missing memoization, heavy imports
3. **TypeScript** — missing types, use of `any`, unsafe casts
4. **Accessibility** — missing aria labels, keyboard navigation, color contrast
5. **Code quality** — dead code, duplicate logic, overly complex functions

For each issue found, provide:
- The file and line number
- What the problem is
- A concrete fix

Be concise. Skip anything that looks intentional or is already well-handled.
