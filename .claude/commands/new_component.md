Create a new React component for this project: $ARGUMENTS

Conventions to follow:
- Use TypeScript with proper interface for props
- Use Tailwind CSS for styling (no inline styles)
- Use lucide-react for icons
- Place the file in src/components/
- Export as named export (not default)
- Use "use client" directive if the component uses hooks or browser APIs
- Keep components focused — one responsibility per component
- Do not add comments unless the logic is non-obvious

After creating the component, show where to import and use it.
